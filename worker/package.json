{
  "name": "the-intern-bot-relay",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "deploy": "wrangler deploy",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.20.1",
    "vitest": "^4.1.10",
    "wrangler": "^4.115.0"
  }
}
