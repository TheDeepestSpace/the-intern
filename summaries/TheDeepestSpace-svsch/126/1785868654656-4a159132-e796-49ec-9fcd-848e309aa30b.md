# Session Summary

- **Timestamp**: 2026-08-04T18:37:34.655Z
- **Target Repo**: TheDeepestSpace/svsch
- **Issue/PR**: #126
- **Backend**: claude

## Prompt / Request
i noticed a couple of odd things about the diagram -- the `instr` bus breakout is not present in the cpu_top diagram which is odd, and `b[]` input port of the `u_pc_adder` is not connected to anything (its declared as `.b (8'd1),`); figure out whats goign on there and report, might be bugs. for this PR i also want you to set up diagram screenshots for each module in the example design to keep it locked in as part of the test suite (png, json, svg, the classics)

## Execution Output
Posted findings and pushed the test commit. Summary:

**Bugs confirmed** (both same root cause): instance port actual-connections that are bit-selects (`instr[7:4]`) or literal constants (`8'd1`) get stringified but never routed through the promotion logic (`getOrPromoteExpr`/`processBusSelect`) that creates bus/literal nodes. Since edges are built by exact signal-name match, this means those ports get **no edge at all** — `control_unit.opcode`, `register_file.rs1_addr/rs2_addr/rd_addr`, and `u_pc_adder.b` are genuinely disconnected in the diagram, not just missing a visual tap box. Verified with a minimal repro plus a direct dump of the parsed graph. Posted as a PR comment with file/line references; left the actual parser fix out of scope for this docs PR.

**Screenshot lock-in**: added `test/visual/example_design.visual.spec.ts`, generating PNG/JSON/SVG snapshots for all 10 example-design modules via the existing visual-regression harness. Lint, unit tests, and the full Playwright visual suite (109 tests) all pass. Changes pushed to `docs/example-design-readme`.
