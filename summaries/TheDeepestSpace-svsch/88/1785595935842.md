# Session Summary

- **Timestamp**: 2026-08-01T14:52:15.829Z
- **Target Repo**: TheDeepestSpace/svsch
- **Issue/PR**: #88

## Prompt / Request
implement this feature as scoped in the issue description above: - New "Cut out" button, shown when 1+ blocks are selected (extend `NodeSelectionToolbar` in `src/webview/main.tsx`, don't gate it behind the existing 2+ check that "Auto Layout" uses). - Clicking it collects all wires (edges) incident to the selected block(s), excluding cut stubs, and cuts them via the existing `cutNet`/`cutNets` message path (same mechanics as the wire "Cut" control in `src/webview/orthogonal/OrthogonalEdge.tsx`) — no changes needed to `mergeNetCut`/`mergeNetCuts`. - Multi-select: clicking "Cut out" on any selected block cuts wires touching any selected block, same batching pattern as the existing multi-wire cut. - Add two BDD scenarios to `test/features/diagram_interaction.feature` (single block Cut out, multi-block Cut out) mirroring the existing "Cutting one wire in a multi-wire selection cuts every selected wire" scenario — reuse existing step definitions for cut-net assertions. Open a PR when done.

## Execution Output
N/A
