# Session Summary

- **Timestamp**: 2026-08-03T14:52:49.535Z
- **Target Repo**: TheDeepestSpace/svsch
- **Issue/PR**: #108
- **Backend**: claude

## Prompt / Request
>When this branch merged master (which already had https://github.com/TheDeepestSpace/svsch/pull/110's graphRegression.ts selector fix), the merge kept this branch's stale pre-fix values for the 12 visual snapshot JSON files instead of master's corrected ones — exact same files, same diff size as https://github.com/TheDeepestSpace/svsch/pull/110, just inverted. Im surprised that this did not cause a CI failure -- that probably means that JSON comparison is not testing the entire JSON for difference 🤔 can you investigate and pop an issue if this is true?

## Execution Output
Confirmed and filed: **#122** — `updateMode === 'missing'` (Playwright's CI default) makes `test_visual`'s JSON/SVG regression checks always overwrite baselines instead of comparing, so they never actually fail. That's exactly why the 12 stale snapshots from #108's merge accident sailed through `test_visual` while `test_bdd`/`test_syntax` (which don't have this bug) correctly failed. Commented on the issue to dispatch a fix, and posted the findings back on PR #108.
