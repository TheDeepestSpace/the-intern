# Session Summary

- **Timestamp**: 2026-08-04T05:31:04.949Z
- **Target Repo**: TheDeepestSpace/svsch
- **Issue/PR**: #127
- **Backend**: claude

## Prompt / Request
fix the race described above — add serialization/an in-flight-promise guard so concurrent extractDesignWithUhdm calls for the same cacheDir never spawn two Surelog processes at once. Add a regression test if practical (e.g. call buildDesignGraph/extractDesignWithUhdm twice concurrently for the same workspace and assert only one Surelog invocation happens / no crash).

## Execution Output
Opened PR #128: https://github.com/TheDeepestSpace/svsch/pull/128

**Fix**: added a per-`cacheDir` mutex in `uhdmExtractor.ts` wrapping the cache-check + Surelog spawn. A concurrent caller now waits for the in-flight run to finish and re-checks the cache instead of racing a second Surelog process against the same output dir.

**Test**: `test/unit/uhdmExtractorConcurrency.test.ts` calls `extractDesignWithUhdm` twice concurrently against fake surelog/backend executables (real Surelog can't be built in this sandbox) and asserts Surelog runs exactly once. Verified it fails pre-fix (2 invocations) and passes post-fix (1).

Typecheck/lint clean; `test/unit` shows no new failures vs. unmodified master (the 128 failures present on both are pre-existing, from missing compiled Surelog/backend binaries in this environment).
