# Session Summary

- **Timestamp**: 2026-08-03T17:03:55.473Z
- **Target Repo**: TheDeepestSpace/svsch
- **Issue/PR**: #100
- **Backend**: codex

## Prompt / Request
the general pattern that i noticed here is that it seems like the cuts are not taken into account when doing the layout via elk.js -- i.e. i see that the actual cut net ends are now getting moved off of each other which was not happening before which i guess is evidence that they are now their own nodes in the elk's sense; but i believe the if we did do the layout while taking those clk nodes into account it would look a bit different i.e. because i see that the nodes that are now only connected to cut net ends are still positioned in the exact same spots that they were places before when we would do routing to/from them from/to other nodes; i think the initial issue might have talked about keeping semantic arrangement there on elk js level, maybe that's what causing this? for the cuts that im observing; for example we have input port a connected to instance ; we cut the wire i would expect the hrizontal layout to be [input port node] -- [cut net end] %bit of space% [cut net end] -- [instance node] i.e. i think by default elk would lay this out in a straight line rather than moving one of the cut net ends below the other ; investigate this

## Execution Output
Confirmed and fixed in `9c600d5`.

- Cut labels were post-layout nodes; ELK never placed them.
- First-open cuts also froze the pre-cut coordinates.
- Cuts now exist before layout and retain placement-only ELK relationships while rendered wires remain hidden.
- Expected horizontal source → cut end → gap → cut end → target layout is covered by regression tests.

Validation:

- Lint passed
- Unit: 326 passed, 1 skipped
- BDD: 89 passed
- Snapshot audit: zero overlaps or stale-label warnings

[PR update](https://github.com/TheDeepestSpace/svsch/pull/100#issuecomment-5169389131). CI is running.
