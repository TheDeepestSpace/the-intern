# Session Summary

- **Timestamp**: 2026-08-02T19:23:55.616Z
- **Target Repo**: TheDeepestSpace/the-intern
- **Issue/PR**: #90

## Prompt / Request
fix the regex per the issue description — anchor the stripping so it only strips genuine leading control tokens right after the mention, not any occurrence of those key=value patterns later in the comment body.

## Execution Output
Opened PR #92: https://github.com/TheDeepestSpace/the-intern/pull/92

The fix anchors the `model=`/`effort=`/`backend=` extraction and stripping to a leading run of tokens right after the `@the-intern-bot` mention (matched in any order), instead of matching anywhere in the comment. Added two regression tests covering the exact issue scenario (literal `model="sol"` in prose) and out-of-order leading tokens with a trailing prose mention of `backend=legacy`. All 15 tests in `parse-trigger.test.js` pass.
